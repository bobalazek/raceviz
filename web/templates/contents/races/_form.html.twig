{{ form_start(form) }}
  <div class="row">
    <div class="col-md-6">
      {{ form_row(form.driver) }}
    </div>
    <div class="col-md-6">
      {{ form_row(form.team) }}
    </div>
  </div>
  {{ form_errors(form) }}
  <button type="submit" class="btn btn-primary">
    Submit
  </button>
{{ form_end(form) }}
